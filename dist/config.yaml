project_name: terraform-provider-kustomize
release:
  github:
    owner: Jstrategia-dev
    name: terraform-provider-kustomize
  name_template: '{{.Tag}}'
milestones:
- repo:
    owner: Jstrategia-dev
    name: terraform-provider-kustomize
  name_template: '{{ .Tag }}'
scoop:
  name: terraform-provider-kustomize
  commit_author:
    name: goreleaserbot
    email: goreleaser@carlosbecker.com
  commit_msg_template: Scoop update for {{ .ProjectName }} version {{ .Tag }}
builds:
- id: terraform-provider-kustomize
  goos:
  - windows
  - linux
  - darwin
  goarch:
  - amd64
  - "386"
  goarm:
  - "6"
  targets:
  - windows_amd64
  - windows_386
  - linux_amd64
  - linux_386
  - darwin_amd64
  ignore:
  - goos: darwin
    goarch: "386"
    goarm: ""
    gomips: ""
  dir: .
  main: .
  ldflags:
  - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}}
  flags:
  - -trimpath
  binary: '{{ .ProjectName }}_v{{ .Version }}'
  env:
  - CGO_ENABLED=0
  lang: go
  mod_timestamp: '{{ .CommitTimestamp }}'
  gobinary: go
archives:
- id: default
  builds:
  - terraform-provider-kustomize
  name_template: '{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}'
  format: zip
  files:
  - licence*
  - LICENCE*
  - license*
  - LICENSE*
  - readme*
  - README*
  - changelog*
  - CHANGELOG*
snapshot:
  name_template: '{{ .Tag }}-SNAPSHOT-{{ .ShortCommit }}'
checksum:
  name_template: '{{ .ProjectName }}_{{ .Version }}_SHA256SUMS'
  algorithm: sha256
changelog:
  skip: true
dist: dist
signs:
- id: default
  cmd: gpg
  args:
  - --batch
  - --local-user
  - '{{ .Env.GPG_FINGERPRINT }}'
  - --output
  - ${signature}
  - --detach-sign
  - ${artifact}
  signature: ${artifact}.sig
  artifacts: checksum
env_files:
  github_token: ~/.config/goreleaser/github_token
  gitlab_token: ~/.config/goreleaser/gitlab_token
  gitea_token: ~/.config/goreleaser/gitea_token
before:
  hooks:
  - go mod tidy
source:
  name_template: '{{ .ProjectName }}-{{ .Version }}'
  format: tar.gz
github_urls:
  download: https://github.com
gitlab_urls:
  download: https://gitlab.com
